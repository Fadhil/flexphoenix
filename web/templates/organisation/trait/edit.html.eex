<h2>Edit trait</h2>

<%= render "form.html", changeset: @changeset,
                        action: organisation_trait_path(@conn, :update, @organisation.id, @trait) %>

<%= link "Back", to: organisation_trait_path(@conn, :index, @organisation.id) %>
