<h2>Edit report</h2>

<%= render "form.html", changeset: @changeset,
action: report_path(@conn, :update, @report),
order_id: @order_id
%>

<%= link "Back", to: report_path(@conn, :index), class: "btn btn-default btn-rounded btn-outline btn-xs" %>
